<section id="requirements" class="py-20 px-6">
  <div class="max-w-3xl mx-auto">
    <h2 class="text-3xl font-bold text-center mb-12">System requirements</h2>

    <div class="bg-gray-50 rounded-2xl border border-gray-200 p-8">
      <div class="grid sm:grid-cols-3 gap-8">
        <div>
          <p class="text-sm text-gray-500 mb-1">Operating system</p>
          <p class="font-semibold">macOS 14.0+</p>
          <p class="text-sm text-gray-500">(Sonoma or later)</p>
        </div>
        <div>
          <p class="text-sm text-gray-500 mb-1">Processor</p>
          <p class="font-semibold">Apple Silicon required</p>
          <p class="text-sm text-gray-500">(M1, M2, M3, M4, or later)</p>
        </div>
        <div>
          <p class="text-sm text-gray-500 mb-1">Disk space</p>
          <p class="font-semibold">~150 MB</p>
          <p class="text-sm text-gray-500">for Whisper model</p>
        </div>
      </div>
      <div class="mt-6 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-500">
          <strong class="text-gray-700">Permissions needed:</strong> Microphone access (for recording) and Accessibility access (for inserting text at your cursor). A guided setup walks you through granting both on first launch.
        </p>
      </div>
    </div>
  </div>
</section>
